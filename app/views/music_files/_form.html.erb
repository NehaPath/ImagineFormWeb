<%= form_for @music_file, html: { multipart: true } do |f| %>
  <% if @music_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(music_file.errors.count, "error") %> prohibited this musicfile from being saved:</h2>
      <ul>
      <% @music_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :audio %><br>
    <%= f.file_field :audio, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
